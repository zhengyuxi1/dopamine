{
  "name": "dopamine-shop",
  "version": "1.0.0",
  "private": true,
  "description": "移动端虚拟购物网站 - 复刻购物APP体验，无需付款",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev:legacy": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run preview:main\"",
    "dev:hmr": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run preview:main",
    "client:hmr": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
